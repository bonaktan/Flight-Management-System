import { Outlet } from "react-router";
export default function SearchLayout() {
    return (
        <div>
            <p>Search View (still tba :3)</p>
            <div>
                <Outlet />
            </div>
        </div>
    );
}
