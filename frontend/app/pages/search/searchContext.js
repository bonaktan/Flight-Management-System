import { createContext } from "react";

export const OverlayContext = createContext({});
export const SearchParametersContext = createContext({});
export const FlightSelectContext = createContext({});
